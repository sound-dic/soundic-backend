const LOGIN_TYPES = ['kakao', 'google'];
const STATUS_TYPES = ['waiting', 'playing'];
const GAME_TYPES = ['auto', 'manual'];
const GAME_LEVELS = [1, 2, 3];

module.exports = {
  LOGIN_TYPES,
  STATUS_TYPES,
  GAME_TYPES,
  GAME_LEVELS
};
